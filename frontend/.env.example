# Server-side base URL of the FastAPI backend.
# Next.js (Server Components, Server Actions, and the SSE/artifact proxies)
# reads this. It is never exposed to the browser.
FASTAPI_BASE_URL=http://localhost:8000
